version: 1
backend:
  phases:
    commands:
      - amplifyPush --simple
 frontend:
  phases:
    commands:
      - npm install
    build:
      - npm run start
  artifacts:
    baseDirectory: build
    files:
      - '**/**'
  cache:
    paths:
      - node_modules/**/*
